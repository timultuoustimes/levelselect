---
version: "0.1.0 (22)"
date: 2026-08-17
title: "Safer tracker regeneration"
summary: "Regenerating a tracker can't quietly cost you progress any more — and you can now paste a checklist you already trust."
---

### New

- **Choose what a regeneration does.** The button now says what it will do — add only what's new, review the changes first, or replace everything — and you can pick a different one for a single run without changing your default. Add-only is the default, because it never removes or overwrites anything you already have.
- **Review changes before they happen.** See what a generation would add, remove and rename, grouped by category, and tick exactly what you want. Nothing is written until you say so.
- **Paste a list.** Drop in a community checklist — a markdown table, or sections of numbered items — and it becomes real tracker categories. No AI, no waiting, and nothing invented: every item comes from your text (list syntax is stripped, names and locations are read out of it, and anything unrecognised is reported rather than guessed at). Imported lists are locked, so a later regeneration can't replace them.
- **Your own notes on any item.** Write whatever you want to remember. Your note is yours — no merge mode rewrites it, and a Replace carries it across wherever the item can still be matched.
- **Rename anything.** Categories and items, including generated ones. A generator's naming is a suggestion; if the game calls them Achievements, call them Achievements.

### Improved

- **Progress follows renamed items.** Generation doesn't name things the same way twice. Previously that could silently reset your checkmarks; now progress moves with any item the merge can still match by name, the review tells you how many moved, and anything genuinely dropped can be kept as a personal goal instead.
- **Long checklists are far shorter.** Items sharing a location group under it once instead of repeating it on every row, and short items lay out in columns — one on a phone, more when there's room.
- **Checkboxes are easier to hit.** Tap targets were smaller than they should have been.
- Timer controls are available with the tracker and a guide open side by side on iPad and Mac.

### Fixed [fixed]

- Tapping a second video in the list now actually changes the player — and each video keeps its own resume position instead of inheriting another's.
- Every part of a playlist remembers where you got to, rather than the playlist remembering only one position across all of them.
- Run logging works on AI-generated trackers. "Log Runs for This Game" was a no-op on exactly the games most likely to want it.
- The Runs section no longer appears twice on iPad and Mac in compact tracker display.
- Generation keeps running when you move around the app, with elapsed time and a clearer sense of how long it takes.
